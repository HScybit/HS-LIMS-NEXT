# Sampleify LIMS

Master uploads at `/bulk_uploads` retain immutable staged cells and reviewed row outcomes. Products, Parameters and MoA update matching records while preserving omitted custom fields; explicit blanks remain explicit. User, Customer and Vendor uploads retain their own creation and permission rules. Plain-text staging sends row identities once per bounded batch, and previews read only the requested row revisions. Mixed Excel values, metadata, original exports and correction history retain typed storage.

Run `npm run benchmark:master-bulk -- stage RUN_NAME` or `npm run benchmark:master-bulk -- commands RUN_NAME` with `SAMPLEIFY_TEST_DATABASE_NAME`, `DATABASE_URL` and `MIGRATION_DATABASE_URL` already pointing to the same local `sampleify_verify_*` database. The benchmark checks every migration hash and both cell foreign keys, refuses the shared database, retains synthetic fixtures, and records three warmups plus thirty samples per shape. Staging includes CSV decoding, authentication and commit; preview is the immediate first read. Fixed local p95 targets are 10 seconds for staging, 1 second for preview and 5 seconds for each 25-row review/process command. Results are written under `.local/master-bulk-performance-*`; these service measurements exclude HTTP/browser time.

Instruments at `/equipments` require configured Instrument Management access and `instruments.read`/`instruments.manage`. Readers see assigned Instruments or records covered by their actual Role's access capability; managers can author the master. The source wizard shows the three fixed service types (Preventive Maintenance, Breakdown, Calibration — see `src/instruments/service-types.js`) and supports current Custom Fields, files, generation and up to 500 access users. Saves retain typed revisions, relationship identities and observed labels, with exact retries after interrupted responses. Lists provide source columns, filters and service health using the viewer's calendar day; details include printable QR labels. Service execution and completed service logs belong to the later laboratory operations scope. Run `scripts/benchmark-instrument-fields.js` and `tests/performance/instrument-fields.spec.js` separately against an isolated synthetic verification database; the browser benchmark requires a `PROFILE_REACT=1` production build.

NABL certification authoring at `/nabl_certificates` uses `compliance.read`/`compliance.manage`. It retains the source validity dates, scope/certificate files and per-parameter Product/MoA selections. Calendar dates stay literal; both sets must be nonempty for a row to display NABL, and each selection must appear on an active Decision Rule for the parameter. Scope rows and choices are paged and searched without discarding drafts. Saves and uploads support exact retries; revisions retain observed reference labels and immutable original file bytes. Editing shows current reference names. Files may be up to 25 MiB and use passive downloads; clearing a field preserves earlier revisions. Delete retires the record from current listings. Authoring does not yet complete active-certificate selection or accreditation behavior in sample/report consumers.

Units are managed at `/unit_management` by users with `users.manage`; `users.read` permits viewing. Code, Name, Description and Active changes use stable IDs, optimistic revisions and exact save-request retries. Inactive units remain visible and can be reactivated. Existing user assignments and saved profile labels are preserved, while new assignments require an active unit. Typed history begins with the first actual save; earlier support rows are not given invented editors or dates.

Labs at `/lab_management` retain the source Name, Abbreviation, HoD/Delegate and four range-text controls, plus PERN Code, Business unit, Description and Active. The source list’s Code column displays the abbreviation; Reference Code displays the separate required code. New labs require four nonempty range entries. Their exact text is preserved without interpreting the range, including zero, prefixes and whitespace; unchanged unknown limits and legacy labels survive later edits. HoD/Delegate assignments are tenant-scoped metadata and do not grant permissions. Delete retires a lab, preserving assignments and immutable revisions; editing can reactivate it. Exact retries return the recorded revision, and stale edits retain the draft. Lab reads/authoring use `users.read`/`users.manage`. Environmental reading entry remains incomplete.

Materials at `/materials` support create/edit dialogs, opening stock, manual IN/OUT/Damaged transactions, transaction tabs and printable QR labels. PostgreSQL serializes stock writes and compares exact decimal quantities; only presentation is rounded. New incoming batch numbers are unique without regard to case, and recent/exhausted batch choices remain searchable. Opening stock keeps its identity while positive, with every change retained in material history. Ordinary transactions are immutable and retain their actual actors and units. Exact retries recover committed saves after lost responses. Existing measurement units support Materials without adding a separate unit-management module. Stock reports, expiry reminders, IQC and automatic method consumption remain open.

Material Categories are managed at `/material_categories` with Name, Description, Reusable and Expirable. The master APIs use tenant-scoped UUIDs, revision checks and save-request IDs, with immutable editor history and exact retries. Active names are unique without regard to case; retirement retains the saved record and allows a replacement category to use the old name. Categories assigned to active materials cannot be retired. Lists support bounded search, Boolean/text filters and inclusive UTC calendar-day filters. The standalone `scripts/benchmark-material-categories.js` and `tests/performance/material-categories.spec.js` measure isolated synthetic workloads.

Sample Category estimates preserve finite fractional days from the original application. Sample registration choices, edit previews and saved reporting dates retain the existing calendar-day truncation; migration `0159` preserves all existing integer estimates exactly.

Product images can be uploaded, previewed, replaced or removed on sample registration and ordinary sample edits. JPEG, PNG and WebP files are limited to 10 MiB and validated by decoding their contents. Files retain their original bytes, checksum and uploader; removing a line reference preserves the immutable file. Upload retries recover the same file after a lost response, and in-flight uploads cannot move between Product rows. Tenant isolation, sample permissions and workflow edit gates apply. Image metadata loads in one batch for up to 100 Product lines. Project-field capture and acknowledgement printing remain separate migration work.

The image service benchmark is `node scripts/benchmark-sample-images.js`; the browser benchmark is `npx playwright test --config=playwright.performance.config.js tests/performance/sample-images.spec.js`. Run them separately with `SAMPLEIFY_TEST_DATABASE_NAME` and database URLs pointing to a synthetic verification database.

Sampleify LIMS uses the Next.js App Router, React, PostgreSQL, Drizzle and the existing Bootstrap interface. Application code and configuration are JavaScript.

The application provides database-backed sign-in, tenant-scoped sessions and permissions, profile edits, password changes, authenticator enrollment and verification, logout and local password recovery. Master Templates includes listing, creation, container/row/column editing, formulas, ordering, cloning and HTML preview. The authenticated entry opens My Account.

The User Management read API at `/api/users` and `/api/users/:id` requires `users.read` or `users.manage`. Lists use three service queries for up to 100 organization members when no custom fields are configured; details use one. Responses include identity labels, assigned roles and the organization's recorded sign-in/sign-out times. Availability combines identity and membership status; `createdAt` records when the organization membership was created, while `identityCreatedAt` records global account creation. The directory exposes the saved default-role and unit labels, a live default-role description and stable sorting. Strict text/relation/status filters are bounded; created-on calendar filters accept an explicit viewer `timeZone`, including DST boundaries. Missing activity remains absent, and revoking a session does not invent a logout event. Credential, session and MFA tables remain private. Run `scripts/benchmark-user-directory.js` alone with an isolated synthetic database configuration to measure the directory service and serialization path.

User lists display configured Additional Data Fields from each member's current capture. Values and `pf:saved_key` filters/sorts follow the saved field key across replacement definition IDs. Dropdown cells retain captured labels; filter choices and date formats use current metadata. Literal global/custom searches include captured display and raw primitives, including zero and false. Custom sorting orders missing, numeric, text and boolean display values with a stable user-ID tie. `row.customFields` contains visible values keyed by saved field key; unrecorded captures stay empty. Metadata, dropdown choices, visible values and raw date values load in batches, adding at most three service queries to the base list. Background metadata refresh and Retry preserve pending status commands. The existing URL request limit still applies;500 configured columns does not imply support for500 simultaneous long filters. Run `scripts/benchmark-user-field-listing.js` and `tests/performance/user-field-listing.spec.js` alone against an isolated synthetic database for the declared listing budgets.

`GET/PATCH /api/users/:id/profile` reads and saves membership-specific employee code, contact number, designation, manager flag, business unit, default role, laboratory, reporting manager and assigned roles. Saves require `users.manage`, a request UUID and the expected profile revision. An unrecorded profile returns revision zero with `profile: null`; its first save requires explicit default role and laboratory selections. Sparse edits preserve omitted fields and hidden assignments, and add the chosen default role to the assignment set. Explicit role inputs are limited to 100; larger existing sets survive unrelated edits. New selections must be active and belong to the organization. Stale or reused requests fail, while exact retries return the original saved revision. Shared global identity and credential fields stay separate. Profile and role commands serialize administration changes and retain the last active user with each existing administration permission.

Profile history records actual editors and the reference labels observed at each save, with links to recorded role revisions where available. `?revision=N` reads that frozen revision; `/api/users/:id/profile/history` pages revision summaries, and `/api/users/profile-references` searches active choices or resolves selected references, including inactive labels. Profile detail uses one service query, history two and references one. Run `node --env-file=.env.local scripts/benchmark-user-profiles.js` alone for synthetic read/save benchmarks including authentication, deferred checks, commit and serialization.

`POST /api/users` creates an account, credential, organization membership, profile and actual creation history in one transaction. It requires `users.manage`, a new user UUID, a request UUID, revision zero, name, username, email, a supplied password and explicit default role/laboratory choices. New accounts receive only their selected roles. Failed reference validation leaves no partial account; exact retries return the original creation outcome after later edits. The creation receipt stores a keyed fingerprint, with no password text or unkeyed password digest. Native creation and My Account username edits coordinate case-insensitive sign-in identifiers across organizations. Existing imported email ambiguities remain representable and do not select an arbitrary account. Run `node --env-file=.env.local scripts/benchmark-user-creation.js` alone for measured creation, retry and collision cases including real password hashing and commit.

Account creation also accepts optional `customFields` and `customFieldTimeZone` within an 8 MiB request. Supplied values join the account/profile transaction and the exact keyed request fingerprint; late field, reference or uniqueness errors roll everything back. Omitting fields creates no capture, while an explicit empty set records revision one when no active definitions remain. Responses include `customFieldRevision` only for supplied captures. Run `node --env-file=.env.local scripts/benchmark-user-creation-fields.js` alone for the combined creation and capture budgets.

`GET/PATCH /api/users/:id/status` reads or changes organization membership availability. Writes require `users.manage`, a request UUID, expected status revision and explicit `membershipActive` boolean. Self-disable and removal of the last administrator are rejected, including concurrent role/profile edits. Deactivation revokes only this membership's sessions; reactivation requires a fresh sign-in. It does not change the global identity gate, credentials, other memberships or recorded logout times. Exact retries return the original outcome after later transitions. Revision zero has no invented status history; `/api/users/:id/status/history` pages actual immutable transitions and saved editor labels. Directory results retain combined `active` and also expose `membershipActive`, `identityActive` and `statusRevision`. Run `node --env-file=.env.local scripts/benchmark-user-status.js` alone for status, revocation and history timings.

`GET/POST/DELETE /api/users/:id/signature` manages a membership's signature file. Uploads require `users.manage`, a binary body, `x-upload-request-id`, `x-signature-revision`, a URI-encoded `x-file-name` and the file's Content-Type. The original Meteor file contract permits arbitrary types, empty files and up to 20 MiB. Removal takes a JSON request UUID and expected revision. Upload/removal revisions retain original immutable bytes, checksums and actual editor labels; exact retries preserve later selections. `/api/users/:id/signature/history` pages metadata without loading file bytes, and `/api/users/signature-files/:fileId` downloads an exact historical file with safe attachment headers. Signature changes leave profile/status revisions and credentials independent. Run `node --env-file=.env.local scripts/benchmark-user-signatures.js` alone for incompressible file, retry, removal and history timings. Adding `?view=1` permits private image/PDF viewing with a sandbox CSP; other types remain downloads. Original bytes and checksums stay unchanged. Print eligibility and historical workflow/report signature binding remain under implementation.

`GET/PATCH /api/users/:id/account` reads or edits global name, username, email and an optional supplied password under `users.manage`. Send a request UUID and the current global account revision. A blank edit password keeps credentials unchanged. A laboratory manager can change an ordinary identity confined to the current organization or their own account; changing another shared or platform identity requires an explicit platform grant in the actor's current membership. Grants are private operator-managed records, independent of editable roles. No grant is installed automatically. New alias collisions are rejected, including username/email cross-collisions; unchanged imported email ambiguities remain representable. Password edits revoke all target sessions across memberships, including the current session for self administration, clear the forced-change gate and consume pending password resets. `/api/users/:id/account/history` exposes actual immutable events from the current organization without hashes or fingerprints. Exact retries preserve later changes and newly established sessions. Receipt authentication uses a domain-separated key derived from `MFA_ENCRYPTION_KEY`; preserve that key with database backups. Run `node --env-file=.env.local scripts/benchmark-user-accounts.js` alone for the declared account timings.

`GET /api/users/:id/form` loads directory, global account, laboratory profile, signature metadata and typed `fieldCapture` in one consistent read. It uses five queries for an absent/empty capture and seven when populated, without file bytes. `PATCH /api/users/:id` saves identity, sparse laboratory-profile changes and optional custom fields in one authenticated transaction. Supply the request UUID, global `revision`, `profileRevision`, name/username/email, optional password and only changed profile fields. Supplied `customFields` also require `customFieldRevision`; captured dates require `customFieldTimeZone`. Field revision/time-zone metadata without fields is rejected. The body limit is 8 MiB. Omitted fields preserve the current capture and recheck current unique definitions against its saved keys, including inactive memberships. Explicit empty fields record a clear only when no active definitions remain.

Invalid references, stale revisions, protected identities, duplicate fields and alias conflicts roll back all form commands and histories. Profile and capture saves precede the final password command, so self administration commits the complete form before ending the session. New private keyed receipts bind the normalized full input and actual profile/capture versions from that transaction. Exact retries preserve later edits and fresh sessions; changed values or added/omitted form parts conflict. Legacy receipts retain their original metadata and only accept compatible retries established by their actual transaction history. Omitted profile fields retain values and hidden roles; `profileRevision: null` means no profile command ran. Signature upload/removal remains separately recoverable. Run `node --env-file=.env.local scripts/benchmark-user-forms.js` and `node --env-file=.env.local scripts/benchmark-user-form-fields.js` separately for declared form, field-size, retry, password and omitted-uniqueness budgets.

User Management at `/user_management` provides the Meteor list/card/status layout, creation, editing and a dedicated `/:id/view` page. Editors preserve hidden role assignments and employee codes, unchanged inactive references and optional blank passwords. Protected identity fields remain locked while laboratory profile edits are available. Unknown saves and status changes retain their exact requests for retry, including table refreshes; permission refreshes disable a mounted editor without discarding its entries. Existing-user signature controls save independently. A new-user signature uploads after account creation, and a failed upload retries without creating a second account. Selected files use passive image or sandboxed previews. Raw locale-text login/logout filtering and complete historical signature binding remain open parity items. Run `node --env-file=.env.local scripts/benchmark-user-screen.js` and the separate `tests/performance/user-screens.spec.js` with the performance Playwright configuration, each alone, for declared service and browser budgets. The service benchmark records explicit `ANALYZE` preparation after populating its synthetic fixtures; its timings do not establish first-read performance after an unanalyzed bulk load.

User creation and editing now render manual Additional Data Fields through the existing Bootstrap controls and save them with the account/profile transaction. Drafts follow saved field keys across metadata refreshes and preserve false, zero, repeated values and original file uploads. Metadata failures retain mounted controls and offer Retry; uploads and unknown saves freeze refreshes and retain their original requests. Current user labels refresh separately from captured historical names, and bounded choice menus offer search for further users. Empty active definitions omit capture writes. Scheme generation remains incomplete. Fresh file drafts also survive a replacement definition UUID when its saved key matches the immutable uploaded definition key.

Run `tests/performance/user-custom-fields.spec.js` and `tests/performance/user-screens.spec.js` with the performance Playwright configuration after a `PROFILE_REACT=1` build. The new synthetic 10/100/500-text-field cases include navigation/API/initialization/rendering and input-frame timings. The existing save observer records the first visible matching filtered row inside the browser, so assertion polling does not extend its measurement. Original failed reports and traces remain local; these budgets do not establish production or full-source parity.

`GET /api/users/custom-fields` reads up to 500 active user-associated definitions under `users.read` or `users.manage`; `?view=list` selects fields configured for listing or filtering. Definitions retain their order and pinned option revisions, using one query plus one batched query when dropdown options are present. These scoped reads grant no master-data access or definition writes. Run `node --env-file=.env.local scripts/benchmark-user-field-reads.js` alone for the declared metadata-read budgets with prepared fixture statistics.

`GET /api/users/custom-fields/lookup-options?sourceId=UUID` returns every current choice for a configured user lookup under `users.read` or `users.manage`, preserving original line IDs, order and typed labels. The catalog contains at most 10,000 choices and is never silently truncated. An optional positive `revision` and `knownOrganizationId` allow an unchanged catalog to be reused only in the same organization; empty or inaccessible catalogs return no choices. Reads use a consistent read-only transaction and grant no master-data access.

User forms load each configured catalog once, with at most four requests in flight, before enabling Save. Revision refreshes reuse prepared choices and retain drafts by saved field key. Failed refreshes preserve mounted values, block saving and offer Retry; permission changes and pending saves discard late replies. Menus with more than 1,000 flat lookup choices render measured visible rows, retain keyboard focus and expose the full result count and positions to accessibility tools. Wrapped labels keep their actual height. Filtered bulk actions still use the complete catalog, and the existing 500-item draft limit rejects oversized selections without losing prior values. Small and asynchronous controls retain their existing rendering and filtering behavior.

Run `scripts/benchmark-user-lookup-options.js` and `tests/performance/user-lookup-options.spec.js` separately against an isolated synthetic verification database. Set both database URLs and `SAMPLEIFY_TEST_DATABASE_NAME` to that database. This browser benchmark requires an ordinary production build with React profiling disabled. Its 100/500-field shared-catalog and ten-source cases measure navigation-to-ready, menu opening and search frames; the service cases include authorization, SQL, assembly and serialization. Both record explicit fixture statistics preparation. These bounds do not establish arbitrary combinations of maximum field counts, catalog sizes and payload sizes. Large menus use the pinned TanStack Virtual adapter; React Compiler must remain disabled for its mutable scroll methods, and lint currently reports that compatibility warning.

`POST /api/users/custom-fields/user-labels` resolves up to 5,000 selected user IDs to current `{id,name}` rows under user read/manage authority. It preserves first-occurrence request order, includes inactive organization members, and omits unknown or foreign IDs. Empty requests use no service query; other requests use one scoped query in a read-only consistent transaction. The endpoint requires origin/CSRF validation and limits its JSON body to 256 KiB. Current names do not replace frozen capture labels. Run `node --env-file=.env.local scripts/benchmark-user-field-labels.js` alone for the five declared lookup budgets against 10,000 synthetic members with prepared fixture statistics.

`GET /api/users/custom-fields/user-options?search=...` returns up to 50 current user choices and `hasMore` under the same user authority. Search uses Meteor's exact name sanitation and the installed `react-select` filter, including literal substring, case and accent behavior against labels and IDs. Search text is limited to 200 characters. The service scans scoped users in 500-row UUID batches within one consistent read, stops after 51 matches, and includes inactive members. Late or absent matches may require several batches; no total-directory cap silently excludes users. A Node-only adapter loads the installed filter while rendered controls use Next's normal React bundle. User-API deployment traces explicitly include the filter's existing runtime dependencies. After building, run `node scripts/verify-user-field-filter-runtime.js` to verify that dependency coverage. Run `node --env-file=.env.local scripts/benchmark-user-field-options.js` alone for the five declared synthetic search budgets with prepared statistics.

User field attachments use `POST /api/users/custom-fields/attachments` and its `/:id` file endpoint. Uploads require `users.manage`, a current user attachment definition and the existing binary upload headers; reads require `users.read` or `users.manage`. Original files from zero bytes through 20 MiB are immutable and remain readable after field edits or retirement. Exact upload retries return the original metadata; changed requests conflict. User and master attachment access remain separate. Run `node --env-file=.env.local scripts/benchmark-user-field-attachments.js` alone for upload, download and retry budgets.

`GET/PATCH /api/users/:id/custom-fields` reads and captures the complete current user-field set using typed immutable rows. Revision zero means no recorded capture; an explicit empty set records a version when no active definitions remain. Saves require `users.manage`, a request UUID and the expected capture revision. History pins definitions, options, original attachments, selected users' observed names, raw primitives and date parser/time-zone provenance. Unique values are checked across all organization memberships, including inactive users, using each capture's saved field key. Renaming a definition preserves earlier keys; reusing an earlier key under a new definition still checks current saved values. Exact retries return their original revision after later edits or retirement. Reads use one header query and two batched value queries when populated; `?atRevision=N` selects an older capture, and `/history` provides bounded revision summaries. Requests are limited to 8 MiB, 500 fields, 500 items per field and 5,000 total items. Run `node --env-file=.env.local scripts/benchmark-user-field-values.js` alone for synthetic capture/read/retry budgets. Run `node --env-file=.env.local scripts/benchmark-user-field-consistency.js` alone to measure saved-key uniqueness with definition and capture history. Capture, creation and user uploads coordinate definition edits before account/organization locks; uploads also coordinate shared request IDs with master attachments. Generation remains under implementation.

User captures retain dropdown primitives and original attachments from the immediately previous capture when a replacement definition reuses the saved field key. Unavailable choices retain their raw value with an invalid interpretation and no fabricated option reference; a later matching option resolves normally. Same-definition older choices retain their actual option revision. A renamed key, cleared intervening capture, another user or a newly supplied unknown value cannot borrow that continuity. Original file bytes and historical labels remain unchanged. Run `node --env-file=.env.local scripts/benchmark-user-retained-fields.js` alone for the seven declared retained-value capture, repeated-save, retry and read budgets.

A user file can be captured for the first time under a replacement definition when its original uploaded definition version has the same saved key. An explicit selection after a clear or for another member in the same organization follows that same rule. Files uploaded under a different key still require the exact immediate capture evidence described above; changing the upload definition's current key cannot create that evidence. Original definition identity, bytes and metadata stay immutable, and master or foreign-organization attachments remain unavailable. Run `node --env-file=.env.local scripts/benchmark-user-file-drafts.js` alone for first captures of 100/500 small-file drafts and the largest retry/read cases, using prepared fixture statistics.

The template foundation supports text, input, paragraph, number, formula, checkbox, date, dropdown and template image widgets. Definitions and captured results use typed relational tables, stable field identities and frozen versions. Samples includes an initial registration form, quick customer creation, list/detail views and a sample's test-request queue. Analysts can be allocated to requests and enter results using autosave, repeated rows, Calculate and Done. Failed saves retain input, revision conflicts require reload, and runtime versions retain their original template and scientific specifications.

Test Parameters at `/test_parameters` includes the source list, form and detail view with laboratory search and measurement uncertainty. Its spreadsheet supports editable headers, rows, columns, keyboard shortcuts and formulas. Dedicated relational tables preserve raw cell text, stable row/column identities and immutable revisions. Edits retain the method, unit and scale settings outside this form; deletion retires the parameter while retaining existing references and history. Failed saves keep the draft, retries save once, and stale revisions require reload. Read-only master users can view the records. New parameter revisions retain the laboratory name observed at save time, including exact retries after a later rename. Current editing shows the current laboratory name. Earlier revisions without a recorded label remain unknown; the migration does not backfill them from current names.

Method of Analysis at `/method_of_analysis` includes the source list, form and details, with name, free-text UUID, description, decimal places, numeric conversion and multiple allowed users. New methods default to four decimal places and NO; explicit zero is retained. User searches are bounded, and selected names remain visible across searches. Edits preserve the internal code and existing parameter links. Immutable relational revisions retain the actual editor and ordered user selections. Deletion retires the method; failed and stale saves retain drafts, and retries do not create duplicate revisions.

Products at `/products` includes the source list, form and view with Name, Description, Abbreviation, Unique Key, Job Template and Tags. The visible key stays editable and unique within the organization. Template/tag searches are bounded, the list matches any selected tag, and selected labels survive lookup failures. Relational revisions preserve actual editors, ordered tags and hidden sample-category links. Deletion retires the product while retaining sample references and history. Interrupted saves/deletions can be retried without duplicate revisions; conflicting edits retain the local draft. Additional Data Fields supports the source text, number, date, date/time, select, checkbox, email, long-text, user, attachment and configured lookup controls, including repeated values. Typed child rows retain raw zero/false/empty/invalid distinctions, definition revisions, option/user/file references and date-zone/parser provenance. Lost-response retries preserve the authored request even after a definition changes. Current field settings control list columns, filters and sorting; historical values remain tied to their saved definitions.

New sample registrations retain the selected Product's available immutable revision, including its saved Custom Fields. Later Product edits and retirement cannot retarget that reference. Existing sample lines and Products without recorded versions retain their captured name and key; missing historical versions are not inferred or backfilled.

Product-detail widgets display recorded Product attributes and Custom Fields in datasheets, report parameter rows and frozen final-result sections. Their Key selects `_id`, `name`, `description`, `key`, `abbr`, `job_template_id`, `tags`, `organization_id`, `user_id`, `created_at`, `updated_at`, or `project_field__splitter__<field key>`. Actual creation metadata is available only when a creation revision exists. Title, Required and Default Value remain designer configuration; the widget does not accept captured values or substitute its configured default for missing Product data. Reports pin their context line when generated, so later sample-line reordering cannot change the output. Whole legacy Product objects, additional legacy metadata and sample-template consumers remain incomplete.

Product context uses one bounded header query and up to three batched field/value queries, selecting only requested keys and the document's actual Product lines. The API shares prepared display strings between repeated widgets. It rejects more than 100 requested lines, 50,000 field headers, 500,000 raw items or 16 MiB of requested text. Missing bound history returns an error; an unknown field key renders empty. The PDF worker reads these projections only under its active lease for the report sample and receives no direct Product-history table grants.

Custom Fields at `/project_fields` includes the definition list, source form, detail view and deletion. It preserves the displayed field types, date formats, conditional controls, zero settings, case-sensitive dropdown option keys and ordered role selections. Field keys are lowercased and unique among active definitions in the organization. Three batched reads load a definition with up to 500 options and edit roles; immutable revisions preserve earlier settings and identities after edits or retirement. Interrupted saves/deletions can be retried, and failed or conflicting saves retain the draft. The “Editable On Report Reissue?” setting is shown for Sample and Sample Product fields; saved values survive association changes, older-client updates and retirement. Definition authoring and Product capture/generation are implemented. Configured lookup selection and capture are implemented for Users, Products and Test Parameters. Report reissue and other sample/master/widget integrations remain incomplete. Created At filters retain the source server's local-day parsing, so deployment must retain and verify the source server timezone.

The internal lookup observation service stores the flat Meteor `DataMasterLine` dependency in typed, immutable revisions. Native source UUIDs map to original master IDs; each observed line retains its original ID, effective typed label and supplied source order. These are separate from embedded template hierarchy identities. A new observation may rename, reorder or omit choices without changing earlier observations. Empty observations, exact retries after later changes and tenant isolation are supported. `lookupSourceId` on a Custom Field is nullable and versioned; omitted bindings and hidden bindings survive field edits. No existing source mapping is inferred, and no standalone Data Master screen or ingestion HTTP route is exposed. Source export/conversion remains incomplete.

User lookup captures resolve requested choices in one deduplicated query across configured sources. Each valid item pins the actual source UUID, observation revision and original flat line ID; historical displays stay frozen. Missing choices can survive only as exact raw values from the immediate same-user saved-key capture, without fabricated references. Reappearing choices use their actual current observation. Source observations serialize with field captures and revalidate the actual observer session and permission after lock waits, including retries and direct application SQL. User authority exposes only current lines of active, configured user fields. Run `scripts/benchmark-user-lookup-fields.js` alone with the isolated database settings below for the seven declared 100/500-field capture, read, retry, retention and recovery budgets; each field selects 10 values from one shared 10,000-line source.

Text, number, long text, date, date/time, checkbox and email fields in Product and Test Parameter forms keep saved and unsaved values by field key across definition replacements. Renamed or removed keys are cleared; historical revisions remain unchanged.

Multi-user fields in Product and Test Parameter forms also retain saved and unsaved selections by field key, including ordered duplicates and explicit clears. User selectors match UUIDs regardless of casing while preserving each retained raw value when another choice is added or visible choices are removed. Current user names remain available for uppercase UUID selections.

Product and Test Parameter user searches match the displayed name and user ID using the source's accent and case handling. Separators removed from displayed names do not match hidden raw text. Searches include inactive members, retain the current name/ID ordering, and return up to 100 matches while indicating whether more matches exist. Large directories are searched in fixed batches within one consistent read transaction.

Product and Test Parameter dropdowns retain unavailable values from the immediately previous capture under the same saved key. Replacement definitions retain the original raw value with an invalid interpretation and no option reference; a matching current option resolves normally. Same-definition older choices retain their recorded option revision. Renamed keys, cleared captures and other records cannot borrow that continuity, and historical values remain unchanged.

Product and Test Parameter attachment values follow the saved field key through definition replacement, including fresh uploads and exact upload retries. Original filenames, previews, bytes and upload provenance stay unchanged. A file must belong to the same definition, have the same immutable upload key, or be present under the same key in the immediately previous capture of that record. Other records and cleared captures cannot supply missing history; matching original upload keys still permit explicit selection. Foreign-organization and private user files remain unavailable through both master services and database guards.

Product and Test Parameter lookup captures also pin typed source/revision/original-line references and frozen labels. Missing choices retain only identical raw values from the immediately previous record’s saved lookup key, with no invented references; reappearing choices use the current observation. Edits omitting field capture and retirement copy the exact historical values and references. Master writers revalidate the actual session and management permission after lock waits, including exact retries and direct application SQL. Both save routes accept at most 8 MiB; captures remain bounded to 500 fields, 500 items per field and 5,000 items in total.

`GET /api/masters/products/custom-fields/lookup-options` and the corresponding `test-parameters` route accept `sourceId`, optional `revision` and `knownOrganizationId`. Master read/manage permission and a current active lookup binding for the requested master are required. Consistent reads return the complete ordered catalog, including typed labels; a known empty observation can also be reused. Definition responses include `organizationId` to prevent cross-organization catalog reuse during refresh. Both forms load shared sources in batches of four, retain lookup drafts by saved key, preserve drafts on failed refresh and freeze refreshes while a save outcome is unknown. Large menus use the same accessible windowed control and complete-catalog bulk actions as User forms.

Run `scripts/benchmark-master-lookup-fields.js`, `scripts/benchmark-master-lookup-options.js` and `tests/performance/master-lookup-options.spec.js` separately against an isolated verification database with all three database URLs and `SAMPLEIFY_TEST_DATABASE_NAME` pointing to that database. They measure capture/history operations, complete catalog service reads, and production form/menu/search frames respectively. Their declared budgets and results remain separate; catalog results do not establish capture-tail, first-operation or combined-maximum performance.

Observations accept at most 10,000 lines, 200-character original IDs, 16,000-character labels and an 8 MiB command. Oversized or unsupported source data is rejected for reconciliation. `scripts/benchmark-lookup-sources.js` measures complete authenticated saves, reads and old retries with one warmup and five samples at 10/1,000/10,000 lines. Select a retained `sampleify_verify_*` database through `SAMPLEIFY_TEST_DATABASE_NAME` and point both database URLs to that database before running it; the benchmark refuses the shared local database. Results are written to `.local/lookup-source-benchmark.json`. These service measurements exclude HTTP and browser rendering.

The Custom Field attachment API accepts a single file of up to 20 MiB for a current Product attachment definition, including an empty file. It stores immutable bytes, filename, media type, checksum and the actual uploader/time with tenant and definition-version references. Exact upload retries retain the same identity; new uploads reject stale or retired definitions. Authenticated downloads remain available after definition edits, and active file types download under restrictive browser headers. The Product form supports upload, exact retry, view/download and clearing the draft reference without deleting historical bytes. Shared value helpers preserve source empty/zero/false and repeated-value behavior; Moment 2.30.1 handles browser-local formatting and server-only Moment Timezone 0.6.3 handles explicit named-zone parsing without changing global defaults. Product values persist in typed relational history; the new Product benchmark commands measure their full form/list/save path.

`GET /api/masters/products/custom-fields` loads current Product field definitions in source display order and their exact version's select options. It requires tenant master read/manage permission, returns at most 500 fields, and reports incomplete or oversized definitions without returning a partial form. Empty or text-only forms use one database statement; select options add one batched statement. An edit between reads cannot replace the selected revision's choices. Hidden nonselect options and definition role settings do not change this source form's controls. The Product form uses this endpoint before enabling Save. Field-load errors retain the base draft and offer Retry. Product history adds two batched captured-value reads; lists use at most six definition/page/value queries, independent of field and row count within the supported limits.

Datasheet submission records the exact frozen capture, selected final result, unit and actual analyst. Sample and test-request workflows support positive any/all/sequential approval with recorded comments and checklist answers. Hidden source request-state identifiers remain separate from display labels through edits, publication and cloning. Done saves entered results; submission and approval are separate actions.

TR Data displays supported scientific markup in datasheets and frozen reports, including subscripts, superscripts and entities. Unbound image markup remains visible as text.

Sample registration defaults Receiving Date to local today and keeps it read-only. Organization Settings → Sample Page → Allow Editing Receiving Date enables explicit dates. Failed saves retain the entered date; older settings clients preserve this preference when omitted. Registration options expose only this flag through the sample permission boundary. The API continues to validate explicitly supplied instants independently of this form setting.

Registration preserves PostgreSQL timestamp precision for receipt and due dates. It validates their order before allocating a sample number, and derives the number year and retention date from the same normalized receiving instant. Timestamp rounding across midnight or a year boundary stays consistent; invalid offsets, reversed dates and UTC years outside 0001–9999 fail without partial registration or consumed numbers.

`PATCH /api/samples/:id` accepts `revision` and explicitly supplied header fields with sample-management and current-workflow edit permission. It preserves omitted headers, applies the IQC/amendment/complaint restrictions, validates changed customer and quotation references, and records the save atomically with its actor. Receiving-date changes preserve PostgreSQL timestamp precision and recalculate retention when the instant changes.

For ordinary samples, an optional `products` array replaces the complete line/test selection. Retain each existing line and test's `id`; omit `id` for new rows. Retained planned tests keep their IDs, and duplicate or foreign IDs fail. Lines used by requests or reports retain their Product/category identity and cannot be removed. Requested tests retain their parameter, method and retest identity; operational metadata may change without rewriting their scientific snapshots. Unchanged selections retain captured labels and may reference inactive masters; changed selections must use valid active references. An optional primary `sampleCategoryId` requires the complete `products` array and recalculates retention. The command commits all changes and one revision/activity event together. Its 8 MiB transport limit matches registration, with at most 100 lines and 5,000 tests. The Edit action opens the existing Bootstrap form at `/samples/:id/edit` when sample-management and current-workflow edit access allow it. The form applies special-sample and receiving-date locks, retains inactive saved selections on their original rows, preserves unchanged header values, and keeps unsaved input after failed or stale saves. Bound captures and project-field editing remain pending.

Reporting dates follow the source server rule: both the stable test-ID selection and its line/parameter/method contents must change. Reordering, metadata edits and replacing IDs with identical contents preserve the date. Calculation starts from the receiving UTC day stored by PostgreSQL, with positive finite rule estimates before category estimates. Rule selection includes inactive and other-category matches in the source order; any rule in the Product/parameter/method sets suppresses per-row estimate fallback. Methodless rules are excluded. Automatic dates preserve an existing same-day due instant and cannot precede receipt; missing or unrepresentable estimates leave the date unchanged.

`scripts/benchmark-sample-reporting-date.js` measures actual recalculation for one test and for 100 lines/5,000 tests with 1,000 matching rules in an isolated synthetic database. Budgets are 250/2,500 ms p95 and at most 40 service queries, with one warmup/five samples. It includes authentication, commit and request/response serialization; fixture setup, loading saved IDs, browser/network and the first operation are excluded.

Test-request generation preserves PostgreSQL precision for explicit due dates and inherited sample dates. Omitted or null overrides inherit the saved sample due date. Invalid PostgreSQL timezones and explicit dates that normalize outside UTC years 0001–9999 return a validation error before generation; no new receiving/due ordering restriction is imposed. Automatically created jobs retain the stored child-date precision.

For complaints, `complaintRetestIds` supplies the complete selection of 1–5,000 existing test IDs. Selection keeps Product lines and test IDs, removes only unused omitted tests, and marks selected unused rows for retest. Requested or otherwise used tests cannot be removed or have their retest identity changed. Duplicate per-line parameter/method selections fail with a validation error. Locked Product and header details are ignored; allowed headers, selection, source reporting-date calculation and one revision/activity event commit together. Complaint generation uses only selected retests, including automatic generation.

`scripts/benchmark-complaint-retests.js` measures changed selections using distinct prepared complaints: two tests/select one (250 ms p95), 5,000 tests/select all, and 5,000 tests/select half (2,500 ms each). Each case has one warmup and five measurements with at most 20 service queries. Authentication, commit and request/response serialization are included; fixtures, verification reads, browser/network and the first operation are excluded.

`scripts/benchmark-sample-edit-form.js` measures edit-model parsing, hydration, a real change, validation and serialization: one test (25 ms p95), 5,000 tests (250 ms), and 5,000 tests with long text (500 ms). `tests/performance/sample-edit-form.spec.js` measures production Chrome loading for one and 100 saved Product lines with one test each (2,500/5,000 ms p95) and input response (500 ms). Both use one warmup/five measurements. Browser timing includes network/API/React and two animation frames; fixture creation, sign-in, first navigation and persistence are excluded. Full 5,000-row browser rendering is outside this measurement.

`scripts/benchmark-sample-header.js` requires an isolated synthetic database through `SAMPLEIFY_TEST_DATABASE_NAME`. It measures header saves for one test and for 100 product lines with 5,000 tests, including a long-text header. Each case uses one warmup and five measurements against a 250 ms p95 budget, including authentication, commit and JSON serialization; browser/network time and line-edit performance are outside this measurement.

`scripts/benchmark-sample-lines.js` measures complete line saves in the same isolation: one planned test has a 250 ms p95 budget; 100 lines and 5,000 planned or requested tests have a 2,500 ms budget, including a long-text payload and requested-test reordering. It includes input/response JSON, authentication, reconciliation and commit, using one warmup and five measurements. Run it alone; fixture generation, browser/network transfer, newly selected references and first-operation latency are outside this measurement.

An assigned analyst can add applicable methods while the sample workflow permits testing. Each method has its own capture and frozen method/template version, retaining the request's frozen parameter, unit and acceptance criterion. The selected method supplies the submitted result and COA. Deleting an open method preserves its datasheet and history; the last active method and methods awaiting review cannot be deleted.

Organization Settings includes the job template, job workflow and automatic-generation choices. Tenant Settings also includes the source Date Format and Date-Time Format choices. Custom saved formats remain available; absent values display the source defaults. Older API clients preserve these preferences when saving other settings. Connecting these preferences to frozen TR Data output remains under implementation. TR Data configuration now includes Title, Key, Required and Default Value. These settings retain their template history; the configured title and default do not replace displayed request data. New captures and repeat clones do not store TR Data defaults, and Required does not demand an entered value for this read-only widget. Existing captured history is retained. Source Key-based runtime resolution is still being migrated. Jobs use the organization's summary template, falling back to each Product's Job Template when the organization choice is unset. Job execution requires an active datasheet template. Manual creation validates every selected product line and rolls back the selection if one is unavailable; automatic generation leaves unavailable groups unassigned. Later Product/settings edits cannot retarget an existing job. Manual jobs group selected requests by sample-product line; automatic jobs group newly generated requests and retain their original child assignments. Parameter-loop sections bind each row to its actual test request and frozen specification. Manual measurement clones preserve those bindings and can copy values. Source Financial Year and Non-NABL start-number controls use five explicit lexical settings columns. Product schemes preserve manual/on-init/on-submit timing, source token expansion, counter padding and ordered field dependencies. On Init runs during initial Save. Generation previews do not reserve counters; configured uniqueness protects actual saves. Native scheme patterns run in a separate worker with a ten-second command deadline; timed-out or invalid schemes return an error and retain the draft.

Numeric result inputs in a job summary retain their entry order, exact saved value and selected child datasheet. Submitting a job records each covered child result from its actual summary or individual capture, including zero and independently submitted results. Clears require correction before submission. Parent workflow completion or cancellation records its effect on the covered children, with the real parent action, actor and time. Child activity links to that job. After an intermediate parent decision, review continues through the job; stale actions from the child's previous graph are blocked. The child's original workflow definition and individual capture remain available as historical evidence.

**Test Reports** opens the COA template selection and saved-revision preview. Consolidated, product-wise and parameter-wise drafts pin submitted results, scientific specifications, report templates and print choices. Report widgets include sample details, test-request data, decision-rule fields, final results and serial numbers. Final-result sections retain repeated rows from the frozen capture. Generation retries reuse their request identity; draft regeneration creates another immutable revision. Definition and capture reads batch across the report's templates and datasheets.

**Finalise** generates finalised reports and completes the sample in one transaction, retaining the actual actor, time and sample revision. A lost response can be retried without completing twice. Finalisation is separate from report issue and preserves the sample's workflow history. Finalisation prevents new report generation, while existing reports remain available for viewing and printing. Exact retries still return their original revisions.

**Print** queues a durable PDF job and opens the browser print dialog when it is ready. A separate restricted worker renders the same frozen template/capture used by the preview. Each attempt retains its actual worker, timestamps and outcome; completed PDF bytes and checksums are immutable. Reopening or repeating Print uses the existing job and artifact for that report revision.

Header/footer management at `/header_management` and `/footer_management` uses the existing rich editor, image uploads, search and pagination. Saves retain immutable content and image versions; deletion preserves report history. Template Settings selects the existing NABL/non-NABL header and footer identities. Generated reports capture their exact content versions, so later edits cannot change earlier output. Report settings use separate read/manage permissions. The source Admin Hub navigation is still being migrated.

Template image widgets retain the source plan-mode chooser, key, width, margins and alignment controls. JPEG, PNG (including APNG), GIF and WebP previews retain the original image and its animation where present; printing uses an immutable first-frame PNG with its recorded orientation. Replacements advance the editable draft, with exact upload retries and typed references preserved by runtime snapshots, captures and report history. Image uploads are limited to 10 MiB, 10,000 pixels per side, 200 frames and 40 megapixels across frames. Original and print bytes share the 24 MiB asset batch and 32 MiB expanded rendering budgets.

Watermark management at `/watermark_report` retains the source list, image preview, opacity slider, dimensions and rotation control. Typed revisions preserve zero opacity, prior image/settings values and actual editors after replacement or deletion. Concurrent edits require the current revision, and interrupted response retries save once. This management screen does not automatically apply a watermark to COAs; the inspected source report callers have no active watermark selection.

This is an initial working flow. Accreditation/ULR handling and report issue remain under development. Full sample editing and variants, additional report asset formats and fonts, allocation resources and qualification checks, remaining widgets, spreadsheet result modes, intermediate job/child workflow synchronization, full rich-text/access/print settings and migration adapters are also incomplete. NABL grouping, rejection timing and conflicting scientific error/numeric-prefix policies remain unresolved; affected actions do not silently choose a different interpretation.

Checklists at `/checklists` includes the source list, form, detail view and deletion, with name search, active-status filtering and up to 200 ordered line items. New forms start inactive; inactive checklists can be edited and reactivated. Stable item identities and immutable relational revisions preserve actual edits, while deletion retains history. Failed saves and deletions keep their retry identity; stale saves keep the draft for explicit reload. `checklists.read` permits viewing and `checklists.manage` permits editing.

Workflow Master at `/workflow_management` includes the source list, Name/Description dialog, cloning, deletion and graph canvas. Managers can add, edit, move and delete nodes, connect their ports, select templates and roles, change badge styles and node actions, and publish valid drafts with Save Flow. Details-only edits preserve workflow type and code. Managers open the draft when available; readers open the published version first. Graph versions are checked against the active workflow and tenant before loading. Source special-role behavior and automatic progression remain under implementation.

Workflow role/template lookups expose only names, IDs and active status to workflow readers and managers. Searches return at most 100 choices; a separate batch resolves up to 500 selected IDs, including inactive selections. Large selections use a bounded POST with normal origin and CSRF protection. Workflow managers can select active templates without receiving template-definition access; concurrent deactivation or session revocation prevents the save.

Partial workflow authoring services preserve omitted node flags, legacy layout, role families, typed conditions and copied checklist history. Explicit edits validate the draft revision and current session after acquiring locks. Role selections update only added or removed assignments; reducing a port count removes its affected draft connections.

The workflow command endpoint atomically creates a draft on the first submitted edit of a published graph and applies that edit. Opening or submitting an unchanged node creates no draft. Typed receipts let an identical request retry return its original outcome after a lost response; changed reuse and stale revisions are rejected. The node editor retains uncertain saves for retry and reloads the current graph after a receipt. A failed read after a known save retries only the read. Stale edits require an explicit reload, and pending forms guard navigation. Persisted drafts offer Save Flow after reload; publication retains the existing strict graph validation.

Mouse and touch dragging preview the node and its connected paths, then save its final coordinates in one command. Scrolling during a drag preserves the grab offset. Clicks, unchanged positions and cancelled gestures create no revision; Escape, pointer cancellation or loss of editing permission discards an unfinished drag. Unknown saves retain their preview and exact retry command; rejected moves restore the loaded position. Stored zero and absent legacy coordinates remain unchanged until an actual move.

Connection forms configure approval and creator roles, Condition, Auto Move, CC emails and Node Checklist. Opening a form creates no draft; unchanged fields preserve sequential stages, hidden conditions, unknown legacy modes and copied checklist history. Changing approver roles follows the source's single-stage assignment; refreshing a bound checklist is explicit. Matching port pairs reopen their existing connection, while multiple existing matches require an individual connection control. Mouse, keyboard and touch controls support exact edit and delete retries. Modals block background canvas focus, and permission changes require a deliberate reload while retaining unsaved input.

Workflow authoring services can select an active checklist master and copy its ordered prompts as required checks, recording the master revision when known. Explicit selection refreshes the copy; omitting the binding preserves it, and explicit null detaches it. Published workflows, both clone commands and actual approval answers retain their saved prompts through later master edits or deactivation. Referenced checklists cannot be deleted. Bounded choices at `/api/workflows/checklists` require workflow read/manage access without exposing Checklist Master contents.

Workflow definitions also preserve the four source Auto Move choices: `yes`, `no`, `all_trs_allocated` and `all_trs_approved`. Historical modes remain unknown when only an older boolean was recorded. Explicit mode changes derive the matching boolean, while older clients retain unchanged conditional choices. Automatic progression and the remaining editor interactions are still under implementation.

Run `npx playwright test --config=playwright.performance.config.js tests/performance/workflows.spec.js` after building to measure synthetic 1/100/1,000-connection graph loading and Chrome rendering. It writes `.local/workflow-screen-performance.json` and requires the dedicated local synthetic database. Run it alone for comparable timings; graph editing is outside this benchmark.

Run `npx playwright test --config=playwright.performance.config.js tests/performance/workflow-dragging.spec.js` separately to measure visible node movement and draft persistence at the same graph sizes. It records event-to-rendered-position timing, command traffic and complete graph preservation in `.local/workflow-drag-performance.json`. First-edit cloning and touch-device latency are outside this benchmark.

The same command with `tests/performance/workflow-references.spec.js` measures role/template lookups over synthetic 1/100/1,000-entry catalogs, including 500 selected IDs and browser response parsing. Results are written to `.local/workflow-reference-performance.json`; picker rendering is outside this benchmark.

Run `node --env-file=.env.local scripts/benchmark-workflow-patches.js` alone to measure partial saves on synthetic 1/100/1,000-connection graphs. It checks hidden data and published-history preservation and writes `.local/workflow-patch-performance.json`. The 500-role case records initial insertion as a warm-up, then measures resubmitting that selection while changing the node name; it does not measure replacement with 500 different roles or browser editing.

Run `node --env-file=.env.local scripts/benchmark-workflow-commands.js` alone to measure draft edits, exact retries and published graph cloning with a first edit over those graph sizes. It writes `.local/workflow-command-performance.json` and checks that published source definitions remain unchanged. These are service measurements; they exclude HTTP transport and editable-canvas rendering.

Run `npx playwright test --config=playwright.performance.config.js tests/performance/workflow-nodes.spec.js` alone after building to measure node dialogs, input rendering and draft saves through authoritative graph reloads. It covers 1/100/1,000-connection graphs and a 500-role selection, preserves hidden definitions and writes `.local/workflow-node-performance.json`. Initial graph loading and first-edit cloning are outside these timings.

Use the same command with `tests/performance/workflow-connections.spec.js` to measure connection dialogs, CC-email edits and saves at those graph sizes, including 500 selected approvers. It writes `.local/workflow-connection-performance.json` and verifies bounded lookup traffic, unchanged approval stages and complete graph preservation. First-edit cloning, touch-device latency and the maximum 100-stage/50,000-role configuration are outside this benchmark.

## Run locally

Requirements: Node **22.23.0**, npm, Docker Desktop and Google Chrome for browser tests.

```sh
nvm use
npm ci
npm run db:local
npm run worker:setup
npm run db:migrate
npm run db:seed
npm run db:seed:demo
npm run build:report-renderer
npm run dev
```

Open `http://127.0.0.1:3000` and sign in as `admin` / `1linkwok@` (a fixed, non-expiring superuser account created by `db:seed` — organization `SAMPLEIFY`, role "System Administrator" with every permission and role capability granted).

`db:seed:demo` provisions a full demonstration laboratory through the real service layer (not raw inserts) — 3 laboratories, 6 roles, 10 operational users, 5 sample categories, a frozen analytical datasheet template, 3 published workflows (sample/test-request/instrument-service lifecycles), module access + organization settings wiring, 4 customers, 3 vendors, 10 products, 10 methods, 14 test parameters, 5 material categories with 8 materials and stock transactions, 8 instruments, 17 decision rules, 3 checklists, 3 service agreements, and 6 demo samples spanning several sample categories. It is idempotent — each entity is looked up by its natural key before creating, so re-running after `db:seed`'s database is otherwise untouched only fills in what's missing. It is restricted to the dedicated local database. Generating test requests automatically for the seeded demo samples is a known follow-up gap (a deeper `analytical_specifications` constraint some product/parameter combinations trip is not yet diagnosed); the samples themselves register successfully regardless. This provisioning pipeline (`src/seed/run-demo-seed.js`) is shared with the platform administrator's "seed demo data" organization-creation option below.

Platform administrators can create and list organizations across the whole deployment at `/administration/organizations`, gated by `is_platform_administrator()` (a `platform_administrators` row for that user's home organization — see `drizzle/0218_platform_administrators.sql`). Creating an organization provisions its first "System Administrator" (every permission and role capability, matching `db:seed`) through `platform_create_organization()`, returns a one-time temporary password, and can optionally run the same demo-seed pipeline for the new organization immediately. Granting platform-administrator status itself is not exposed through any application API — run `npm run db:grant-platform-admin -- <username>` against the owner connection.

Before creating new samples, select published sample workflows in **Organization Settings → Workflow Configs**. The six source settings are Base Sample, IQC Sample, ILC Sample, PT Sample, Amendment and Complaint. Current source registration maps ILC/PT forms to Base Sample and intralab samples to IQC; preserve those mappings. Missing or unavailable assignments block new registration and roll back its rows and counters. Existing samples keep their recorded workflow versions. The demonstration seed does not assign organization defaults.

The settings API accepts an optional complete `sampleWorkflows` object with `base`, `iqc`, `ilc`, `pt`, `amendment` and `complaint` UUID/null values. Omit the group to preserve it; send explicit nulls to clear selections. Saves use the settings revision and actual actor, validate newly changed references, and retain unchanged unavailable choices. The workflow service benchmark (`scripts/benchmark-organization-sample-workflows.js`) and production Chrome benchmark (`tests/performance/organization-sample-workflows.spec.js`) measure 25/1,000 published choices on an isolated synthetic database. They do not measure maximum sample payloads.

Select **Test Request / Job Workflow** before allocating new requests or creating assigned Jobs. The shared source control saves both organization keys; missing or unavailable selections roll back allocation, including child captures and automatic generation. Allocators can consume these settings without settings access. Existing workflow runs retain their recorded versions. Migration 0165 leaves old native requests marked `NULL` to preserve their earlier category/default behavior; new requests default to dynamic workflows, and explicit `false` is reserved for imported source legacy records. The application cannot change that marker. The API preserves the request key when `testRequestWorkflowId` is omitted, and accepts a UUID or explicit null when supplied. Retained unavailable selections remain visible and may be synchronized by the shared control, but cannot start new runs.

`scripts/benchmark-organization-test-request-workflows.js` measures settings load/save, allocation and one-child Job creation at 25/1,000 published choices. `tests/performance/organization-test-request-workflows.spec.js` measures the common control in production Chrome. Both require an isolated synthetic database; larger Job groups and maximum sample payloads need separate verification.

`db:local` creates the dedicated `sampleify-next-local-postgres` container and `sampleify-next-local-postgres18` volume using PostgreSQL 18.6 on **127.0.0.1:55442**. It generates private `.env.local` credentials. Repeating the command starts the same database without removing data. Existing unrelated resources are rejected. Stop this database with `docker stop sampleify-next-local-postgres` when it is no longer needed.

Run `npm run worker:reports` in a separate terminal for PDF printing. `worker:setup` provisions `sampleify_report_worker` before migrations and saves its private connection in `.env.worker.local`. The worker has no application/owner role membership and cannot write analytical records. Supply only `WORKER_DATABASE_URL` to the worker in a managed environment; never supply schema-owner credentials to a web or worker process.

`npm run build` also builds the report renderer. During development, rebuild it and restart the worker after template-rendering, PDF, style or dependency changes. Private renderer artifacts live under `.local/report-renderers/`; their identifier hashes the bundled rendering code, stylesheet and dependency lock. A worker processes only its matching release. Drain old queued jobs with the matching worker/dependencies before retiring a release; retain generated PDF artifacts and database backups. Deploy the renderer artifacts alongside the web and worker release. Without a worker, jobs remain queued; closing the browser does not cancel them. Transient failures retry with delays up to five attempts. A terminal failure requires correcting its cause and explicitly regenerating the report to create a new revision.

PDF rendering uses installed Google Chrome, permits only captured inline resources and has a 30-second rendering limit and 50 MiB output limit. Static PNG/JPEG/WebP/SVG uploads are decoded and limited to 10 MiB, 10,000 pixels per side and 40 megapixels. SVG retains its original vector bytes; its static profile supports shapes, text, gradients, styles and local references, with limits of 20,000 nodes/expanded references and 100 nesting levels. Executable markup, foreign content, animation, external resources, XML document types, CSS escapes/comments and unsupported effects are rejected. SVG is revalidated on image and report reads. A render batch permits 24 MiB of distinct image bytes and 32 MiB of expanded content. Header/footer heights use the selected paper width and orientation; margins retain the source CSS-pixel units. Chrome's additional page-header padding is removed so it cannot overlap the report body. The renderer loads captured assets in one batched read, in addition to the eight core definition reads and up to three capture reads. It does not fetch image URLs from the network. Report HTML/render models exist only in memory; authored rich content is retained in immutable content versions alongside typed image references. CKEditor assets and their license are retained under `public/ckeditor`.

Password recovery uses a local capture adapter: messages are written to `.local/mail/` and never sent externally. Open the URL from the generated text file to complete a reset. The adapter only runs with a loopback application origin. External delivery and production configuration require a separate integration.

## Local verification

```sh
npm run lint
npm test
npm run build:report-renderer
npm run test:integration
npm run test:migrations
npm run build
npm run test:e2e
# Run lint, unit, integration, build and browser checks together:
npm run verify
```

Browser tests launch the production build at `http://127.0.0.1:3100`, using installed Google Chrome and new synthetic accounts. The Print test starts a separate worker, verifies the actual PDF blob and checksum, and observes the native print call without opening a headless print dialog. Native dialog/print-layout checks use visible Chrome separately. Integration tests accept only the dedicated local database. Test data remains available for inspection; tests do not truncate or drop tables. Browser traces and reports are ignored by Git.

`test:migrations` creates a fresh synthetic database in the same local PostgreSQL container, applies the full migration chain twice, and checks authentication, MFA enrollment/retry/disable, template capture, parameter uncertainty and method/user history and retirement, Product Custom Field capture/generation/listing, customer creation, sample registration, allocation and grouped result approval using the restricted application role. The restricted worker produces PDFs for individual final sections with captured Product widgets and for grouped results. The command retains that database and records its name in `.local/migration-verification.json`; it never resets an existing database.

## Database changes

The canonical schema is in `src/db/`: identity, template, master, workflow, sample and report schema files. Generate reviewed SQL with `npm run db:generate`, then apply it with `npm run db:migrate`. Custom SQL migrations hold row policies, immutable-version guards and restricted functions. Applied migrations receive forward corrections, never edits. Drizzle's generated JSON files are schema-tool metadata on disk; no application database column uses JSON or JSONB.

`DATABASE_URL` must use the restricted `sampleify_app` login. `MIGRATION_DATABASE_URL` uses a separate schema owner; it is only needed by migration and synthetic seed commands. The local setup provisions these roles. An independently provisioned database needs those roles before migration. Never run the web application with schema-owner or superuser credentials.

Authentication functions resolve opaque session tokens before setting transaction-local tenant context. Tenant tables use row policies and composite foreign keys; domain services must also check permissions. Arbitrary SQL and caller-provided tenant context must never be exposed through application endpoints. Credentials and sessions have no direct runtime table grants.

Template loads use eight fixed data queries including version selection; capture history adds three. Datasheet loads add one metadata query, plus one batched context query when data widgets need it. Image widgets add one batched asset query; reports share that query with their captured headers, footers and stylesheet assets. Exact historical job selections share the third capture query. Authentication, transaction setup and write authorization are counted separately. HTTP models omit repeated scope metadata and duplicate expression encodings. These objects are derived transport, with no separately persisted document or render cache. Database statements time out after 30 seconds and failed transactions roll back.

Capture reads select revision keys from the history index before fetching typed value payloads. Occurrences are filtered at each requested capture revision before the 200,000-value batch limit, so removed rows do not consume that limit. Explicit historical job selections still retrieve their recorded values after a row is removed.

New capture revisions have immutable records of their full PostgreSQL transaction ID, actor, database role, time and status. Runtime value and repeat changes must belong to the transaction that created their revision; captured values also require the real save actor and time. This prevents later writes from changing a committed revision. Captures created before this boundary retain their original evidence, with revision records beginning at their next actual change.

Set `APP_ORIGIN` to the exact browser origin. Mutations require that origin and authenticated requests also require a session-bound CSRF token. Sessions expire after 12 hours, with at most five active sessions. Remember me preserves the source checkbox behavior without extending that duration. Passwords retain the source's eight-character minimum; password changes revoke other sessions, and resets revoke all sessions. The MFA encryption key must be 32 cryptographically random bytes encoded as hexadecimal and kept server-side.

Account-creation fingerprints use a separate HMAC key derived from that existing MFA key with a fixed domain. Preserve the key with the database backup: changing it also prevents verification of earlier credential-bearing creation retries. The key, passwords and credential hashes must not be included in logs or performance reports.

My Account supports the source MFA switch, local QR code and setup key, authenticator verification, and disable confirmation. Setup expires after ten minutes (or the session expiry if sooner), is encrypted at rest and belongs to the session that created it. Password changes invalidate pending setup. Failed verification is limited to five attempts per account in fifteen minutes, including concurrent requests and setup restarts. A successful enrollment consumes its TOTP step; use the next code when signing in again. Setup/change identities and revisions protect retries and stale dialogs. Disabling removes the stored factor secret. Existing signed-in sessions remain active when MFA changes, matching the source behavior. QR and setup responses are not cached; QR generation uses the local `qrcode` package and no external service.

## Template performance checks

```sh
npm run benchmark:templates
PROFILE_REACT=1 npm run build
npm run benchmark:browser
# Runtime API and expanded datasheet browser measurements:
npm run benchmark:datasheets
npm run benchmark:datasheet-browser
# Product Custom Field service, list, generation and form measurements:
npm run benchmark:product-fields
npm run benchmark:product-fields-browser
# Recorded Product projections and allocated datasheet consumers:
npm run benchmark:product-context
npm run benchmark:product-context-browser
# Restore the ordinary production build afterward:
npm run build
```

The server benchmark creates synthetic small, large and nested templates in the isolated local database. It records 30 read/save/freeze runs and 1/5/20-session read loads, including SQL, assembly, serialization, formula timing and payload size. `npm run benchmark:templates -- --reuse` measures the previously generated fixture identities again and retains the prior report; this requires that the fixture templates have no unfinished drafts. Run without `--reuse` to generate fresh fixtures after a browser benchmark, which leaves edited drafts for inspection.

Each browser benchmark requires its completed server fixture report and a profiling build. It measures 30 loads locally and with 4× CPU throttling, 150 ms network latency and limited bandwidth, plus saves. Runtime measurements also separate input response and React commits from API loading, parsing and initialization. Run benchmarks without concurrent builds or tests. Reports stay in `.local/`; these synthetic measurements do not establish source-application or production performance.

The runtime server report includes individual statement timings and app-role query plans captured immediately after warm and concurrent reads. Plans run outside timed samples, before the next fixture changes database statistics. Individual statement timings include client and transfer overhead; query plans record database execution time separately.

Never commit `.env.local`, `.env.worker.local`, `.local/`, credentials, database dumps or browser traces. No GitHub Actions service is required.

Instrument service types are a fixed set of three (`preventive_maintenance`, `breakdown`, `calibration` — see `src/instruments/service-types.js`), not an Organization Settings tab; there is no org-configurable list or Active flag. Instrument execution, service schedules and legacy import reconciliation remain under implementation.
